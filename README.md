# CADP - parte 2
## anotaciones sobre vectores
- tener en cuenta la dimension logica cada vez que se hace un cambio en el vector.
- maximos y minimmos en vectores:
    * inicializacion con el primer elemento (el primer valor o posicion: "v[1]").
    
